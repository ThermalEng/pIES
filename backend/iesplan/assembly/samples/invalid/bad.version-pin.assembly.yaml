# 非法样例: 设备模型未固定精确版本(未版本化别名)→ ASM-SYN-007
schema: ies.assembly
schema_version: "1.0.0"

assembly:
  id: bad_version_pin
  name: 引用未固定版本

time_axis:
  start: "2025-01-01T00:00:00Z"
  end: "2025-01-02T00:00:00Z"
  resolution: 1h
  endpoint: left_closed_right_open

resources:
  datasets: {}

devices:
  hp1:
    model: ies.device.heat_pump
    parameters:
      rated_heat_kw: 600

connections: {}

constraints: {}

calculation:
  mode: fixed_operation
  generator: acme.generator.highs_milp@1.0.0
  solver: ies.solver.highs@1.7.2

outputs:
  series: []
  metrics: []

extensions: {}
