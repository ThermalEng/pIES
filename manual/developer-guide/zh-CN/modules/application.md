# 应用用例

> 文档状态：生效蓝图；目标代码边界：`backend/iesplan/application/`；迁移边界：`backend/iesplan/services/`

## 作用

`application` 把多个独立领域能力组织成用户能够完成的一次业务动作。例如“提交计算”不是单个模块函数：它需要授权、校验项目、固定版本和数据、生成快照、创建任务、附加对象引用并写审计。

应用层是跨模块协调者，不是新的全局业务所有者。每个事实仍由对应领域模块拥有。

## 边界

应用层负责：

- 接收已认证主体与明确用例命令；
- 执行业务授权和用例级前置条件；
- 调用多个模块的公开门面；
- 拥有数据库事务、幂等和跨资源恢复边界；
- 把领域结果映射成与传输无关的应用结果；
- 记录审计、创建任务、协调对象引用和生命周期。

应用层不实现设备公式、单位换算、求解算法或 BlobStore 路径，也不接触 HTTP Cookie、React 状态和模块私有 ORM。

## 输入与输出

| 输入 | 说明 |
|---|---|
| `ActorContext` | 用户 ID、角色和已验证会话上下文，不是原始 Cookie |
| 用例命令 | 业务字段、资源 ID、revision/幂等键和必要选项 |
| repository/模块门面 | 由组合根注入或在明确边界提供 |
| 当前事务 | 由用例拥有，供 repository 执行 flush/savepoint |

输出是应用结果、不可变资源、任务受理信息或明确错误。它不应包含 HTTP Response、ORM 实例、文件句柄和前端组件形状。

## 一个用例的标准结构

```text
接收 ActorContext + Command
    ↓
加载目标资源并授权
    ↓
校验 revision / 幂等 / 当前生命周期
    ↓
调用领域公开能力
    ↓
协调持久化、对象引用、任务与审计
    ↓
提交事务
    ↓
返回 ApplicationResult
```

任何一步失败都必须回到可解释状态。repository 只能 flush、savepoint 或返回错误，不能在下层擅自回滚调用方的完整事务。

## 用例族

| 用例族 | 主要协调内容 | 典型输出 |
|---|---|---|
| identity | 用户、凭证、会话、角色与认证审计 | 主体或会话结果 |
| projects | 项目、成员、草稿、版本、归档与所有权 | 项目 read model |
| datasets | 上传、质量、版本、周期序列预备、默认预测训练/生成、变换回执、对象引用与项目绑定 | 原始数据版本或项目基线下全周期计算序列 |
| modeling | 项目设备、properties、interfaces、连接、存量/新增身份、revision 与即时校验 | 项目图修订 |
| planning | 目标函数/权重、规划变量、上下界和规划/系统约束 | 规划配置 revision/诊断 |
| finance | 规划与财务计算共同使用的设备单价、O&M、能源价格、税率和资金时间成本 | 财务配置 revision/诊断 |
| calculation | mode、generator、solver、精度、容差、选项、种子和输出选择 | 计算配置 revision/诊断 |
| validation | 汇总项目基线、模型、已预备数据、规划配置和公共财务配置 | 项目校验报告 |
| tasks | 快照、幂等、任务、取消和重试 | 任务受理与状态 |
| results | 证据、评估、候选选择与历史解释 | 固定来源的结果视图 |
| exports | 报告/项目包任务、对象引用与下载授权 | 导出资源 |
| operations | 模块健康、容量、备份和维护协调 | 聚合运维状态 |
| customizations | 模型/插件草稿与版本、共享申请、逻辑安装、停用和可选择目录 | 用户模型/算法目录与共享状态 |

`customizations` 只拥有用户目录和生命周期用例。设备 YAML 的语义校验调用 devices 公开门面，算法插件 contract 与自检调用 computation/runner 公开能力，字节和引用调用 storage；不得在应用层复制这些规则。共享园地安装只创建 `CatalogInstallation` 与对象 owner 引用，不复制底层内容。

## 典型示例：保存项目模型

输入是项目、候选模型 YAML、临时配套文件、基础模型 ID、预期项目 revision 和幂等键。应用层依次：

1. 授权并确认项目草稿可编辑；
2. 核对 revision、候选字节摘要和临时文件归属；
3. 调用 devices 对候选模型及数据文件完成解析、类型、技术语义和方程校验；
4. 有任何 blocking 诊断时直接返回完整诊断，不写项目目录、不登记模型、不分配正式编号；
5. 校验通过后在项目范围内原子分配只递增、不复用的 `_N` 后缀；
6. 用最终 ID 重新完成身份校验，生成规范 YAML、内容摘要和回执；
7. 原子提交模型文件、配套文件、项目模型清单引用和审计，并返回新的项目 revision。

编号分配、文件提交和清单登记属于同一用例的一致性边界。并发请求不能得到相同编号；失败事务不能留下半文件、孤立引用或对用户不可见的已占编号。底层对象存储与数据库无法组成单一事务时，必须使用临时 owner、幂等 finalize 和 reconciliation 达到相同可观察结果。

## 典型示例：预备项目计算序列

输入是项目 ID、项目 revision 和尚未完成预备的项目模型实例。该用例不接收时间轴、重采样、预测算法或算法参数配置，而是直接使用项目创建时已经固定的计算基线：

1. 授权并固定项目计算基线摘要以及相关模型实例 revision；
2. 枚举模型实例中的 `predefined` interfaces，并分别解析 `constant/data_repeat/data_predict` 来源；
3. 将常量确定性展开，将周期数据按原始采样间隔转换并重复到完整周期；
4. 对预测数据校验训练输入、训练目标和预测输入，调用系统固定默认算法及精确版本完成训练和全周期预测；
5. 校验每个输出的单位、有效区间、点数和从零开始的连续 `step`，生成不可变计算用序列文件与预备回执；
6. 输出文件与回执提交成功后，原子地把对应项目模型实例的数据引用替换为该计算用序列文件的内容寻址引用，并形成新的项目 revision；
7. 只有全部预定义接口都指向与当前基线摘要一致的已预备序列，项目才可以进入 assembly。

固定默认预测算法的学习参数和系数保存在不可变训练产物中，预备回执固定算法 ID/版本、种子、训练/预测输入摘要、训练产物摘要和输出摘要；它们不写入设备模型，也不形成用户可选配置。任一输出、回执或实例引用更新失败时，不发布该次替换；已经成功且仍与当前不可变基线一致的其他引用不需要重算，但装配必须报告所有尚未完成的接口。

## 开发一个新用例

1. 用一句话写出主体、动作、目标资源和成功结果；
2. 列出每项事实的所有者，避免在应用层复制领域规则；
3. 定义 Command、Result、权限、revision 和幂等语义；
4. 画出事务内写入、对象操作和异步任务的顺序；
5. 为跨数据库/对象系统失败设计重试或 reconciliation；
6. 先写 application 测试，覆盖成功、权限、冲突和中途失败；
7. 再由 API 或 Worker 适配，不让传输细节回流。

## 典型示例：提交计算

输入是用户、项目 ID、任务类型、配置选择和幂等键。应用层依次：

1. 确认用户有提交权限且项目可计算；
2. 读取并固定项目版本、数据版本和配置版本；
3. 调用 assembly，阻断所有 blocking 诊断；
4. 固定用户选择的计算配置，校验 mode、generator、solver、精度、选项与输出能力；
5. 生成包含 `ValidatedAssemblyArtifact`、设备内容摘要、方程 contract、规划/财务/计算配置 revision 与摘要、资源摘要及 generator/solver/executor/result adapter 精确版本的 `CalcSnapshot`；
6. 以项目作用域幂等键创建逻辑任务；
7. 附加快照对象引用并记录审计；
8. 提交后发布可重建队列消息；
9. 返回任务 ID、状态、assembly 摘要和实际快照摘要。

Application 不生成 Solver Bundle。计算 Worker 在同一冻结快照上把 `ValidatedAssemblyArtifact` 与计算配置一起交给纯 GeneratorProvider，完成计算配置能力校验并封存 Bundle 后交给 SolverRuntime；这样 HTTP 事务不承担求解器文件生成，也不会让 Worker 回读项目草稿。

队列发布失败不能让数据库中的任务变成不可解释孤儿；恢复器应能从权威任务状态重新入队。

## 失败语义

- 主体无权限：在读取敏感内容前失败；
- revision 过期：返回冲突及当前 revision，不覆盖；
- 领域校验失败：保留完整诊断，不继续下游写入；
- 幂等重复：返回同一逻辑结果，不重复副作用；
- 对象或外部写入中断：事务回滚或留下可 reconciliation 状态；
- 内部异常：应用事务回滚，审计和日志保留追踪 ID。

## 必须遵循的规范

- application 是事务唯一所有者；
- 授权必须发生在后端用例中，不能信任前端隐藏按钮；
- 不直接导入其他模块私有 repository、persistence 或 ORM；
- 长任务只创建任务和快照，不在请求线程执行；
- 事件只用于不要求立即一致的后续动作；
- 返回结果与 HTTP 无关，并且不暴露内部路径或秘密。

## 完成标准

- 成功、无权限、不存在、冲突、幂等重复和中途失败均有用例测试；
- 事务中没有下层全局 rollback；
- 对象引用、审计和业务资源生命周期同步；
- 一个用例可以被 HTTP、Worker 或命令行适配复用；
- 当前 `services/` 中的职责迁移有明确归属，新增代码不继续形成无边界杂物层。

阅读当前代码时，从对应业务 service 的公开函数与 API 调用关系入手，并按本章判断它最终应归属的 application 用例；迁移完成后从 `application/<use-case>/` 的命令、结果和 handler 开始。
