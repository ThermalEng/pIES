---
title: pIES 设计输入（洁净版）
format: rpd
version: 2.0.0
status: final-constraints
language: zh-CN
---

# pIES 设计输入

## 1. 文档定位

本文是 pIES 的独立设计输入，集中描述产品范围、用户权限、数据约束、计算语义、结果要求、部署边界和软件架构约束。

- 本文可单独阅读，不依赖其他文档、目录、链接或运行环境说明。
- 条款使用“必须”“只能”“不得”“应”表达约束；“可以”表达允许的能力。
- 本文只保留产品与设计的最终约束，不包含讨论过程、实现任务、编排规则、验证记录或未定方案。

## 2. 产品边界

### 2.1 产品定位

pIES 面向设计院工程师、能源服务公司工程人员、园区能源工程师和单体建筑能源工程师，用于小型工业园区或单体建筑的综合能源系统规划。

用户输入建筑或园区条件、能源设备、时序数据、经济参数和环境参数，获得可解释、可追溯的设备配置、运行能流、财务、环境、工程和可靠性结果。

### 2.2 规划范围

产品只提供规划能力：

- 建立电、热、冷综合能源系统模型。
- 管理并校验年度时序数据。
- 对当前方案进行全年运行评价。
- 在变量、目标和约束下搜索规划方案。
- 评价财务、运行排放、工程指标和可靠性。
- 查看结果、固定证据和导出报告。
- 导入、导出和管理完整项目包。
- 提供程序内快速帮助和独立教程页面。

产品不提供：

- 生产运行计划。
- 实时调度。
- 设备控制或控制指令下发。
- 浏览器内正式求解。
- 产品版本 2 的问答式信息收集、AI 报告生成、AI Excel 标准化和本地 Open WebUI 对接。
- 产品版本 2 的风电、燃气内燃机、余热锅炉、吸收式制冷机、热储能和冷储能设备。

规划任务中的逐时设备行为、能流、财务计算、环境计算和工程计算属于同一次规划计算的内部结果，不构成独立的运行控制业务。

### 2.3 核心不变量

1. 浏览器不是业务规则、项目数据或计算结果的权威来源。
2. 项目草稿可以修改；项目版本和计算快照不可变。
3. 每次计算必须绑定完整输入、数据版本、程序与受控扩展版本、随机种子、容差和内容校验信息。
4. 物理有效性、最优性、财务有效性和可靠性必须分别表达，综合摘要不得掩盖任一维度的失败。
5. 任何数据、模型、算法或扩展都必须经过受控注册、权限校验、输入校验和证据固化。
6. 大型不可变对象必须按内容寻址保存；业务引用和权限边界不能因内容相同而被绕过。
7. 用户上传内容不得直接变成任意服务器代码或动态库。

## 3. 用户、权限与会话

### 3.1 规模与角色

| 项目 | 约束 |
| --- | --- |
| 授权用户 | 最多 50 人 |
| 同时在线用户 | 最多 2 人 |
| 内置角色 | 管理员、工程师 |
| 默认账号 | 首次部署提供内置管理员账号和初始密码 |
| 初始密码 | 默认管理员首次登录必须修改 |
| 自助注册 | 默认关闭；开启时只能注册工程师 |
| 外部身份服务 | 不接入外部身份提供方，不自动映射外部角色 |

### 3.2 项目权限

| 角色或身份 | 能力边界 |
| --- | --- |
| 项目所有者 | 编辑项目、创建版本、提交计算、应用结果、导出 Excel、导出完整项目包、管理查看者、转移所有权 |
| 查看者 | 查看模型和已有结果、导出 Excel；不能编辑项目、提交新计算或导出完整项目包 |
| 管理员 | 管理工程师账号和安全设置；通过维护入口只读查看项目诊断、执行解锁和所有权转移；不得直接编辑能源模型或财务参数 |
| 非授权用户 | 不可见项目，不得读取项目数据 |

补充约束：

- 每个项目只有一个所有者，默认不对其他用户可见。
- 所有者只能授权查看，不能授予共享编辑权。
- 协作编辑通过所有权转移或复制项目形成独立副本。
- 所有权转移后，原所有者默认成为查看者。
- 被停用所有者的项目只能由管理员通过明确、受审计的维护操作转移给有效工程师。
- 查看者可以导出结果型 Excel，但不能导出包含完整模型、数据和历史记录的项目包。

### 3.3 单活动窗口

- 同一账号在同一时间只能拥有一个活动窗口级会话。
- 新窗口或新设备接管时，旧窗口停止接受新操作。
- 已进入服务端保存队列的修改必须先完成处理。
- 新窗口必须重新加载服务端最新修订后取得使用权。
- 旧窗口凭证立即失效并退出登录，不能继续查看或编辑。
- 旧窗口尚未送达服务端的本地修改必须明确提示其可能丢失。
- 旧窗口无响应时，可以在短暂超时后强制接管；强制接管必须提示未送达修改风险。

### 3.4 安全控制

- 密码必须使用安全哈希保存，且满足最低强度要求。
- 初始密码必须强制更换。
- 会话必须过期；浏览器凭证必须使用安全 Cookie。
- 必须启用跨站请求伪造防护、登录限速和凭证失效机制。
- 登录、登出、登录失败、密码修改、密码重置、账号停用、会话接管和权限变化必须写入认证审计。
- 用户响应不得泄露堆栈、数据库语句、服务器路径、令牌、密码或敏感配置。

## 4. 部署与离线能力

### 4.1 部署形态

- 采用浏览器访问的 B/S 应用。
- 以 Docker 方式部署在 Linux x86-64 主机。
- 目标硬件约为 8 核 CPU、32 GB 内存、100 GB 可用存储。
- 核心功能不依赖 GPU。
- 产品版本 1 优先保证单机可维护性，不要求业务微服务拆分或高可用集群。

### 4.2 网络边界

- 主程序、数据服务和计算服务在本地部署环境内运行。
- 不默认依赖云数据库或云文件存储。
- 不默认向外部服务发送项目数据或遥测数据。
- 外部天气、公共数据和其他联网更新属于可降级能力，不得成为已缓存数据和用户上传数据可用性的前置条件。

### 4.3 断网行为

当互联网不可用但本地服务器和局域网仍可用时，以下能力必须继续可用：

- 建模和参数编辑。
- 数据校验。
- 方案评价和规划计算。
- 结果查看。
- 快速帮助、基础教程和恢复说明。
- Excel 导出和完整项目包导出。

系统必须明确显示离线状态，以及天气或公共数据等外部能力的不可用或降级状态。

### 4.4 持久化与迁移

- 容器重建不得丢失业务数据。
- 完整部署数据迁移必须保留项目、版本、数据集、历史结果、证据和审计所需的业务关联。
- 数据库与大型对象清单必须形成一致备份点。
- 恢复后必须校验对象引用和内容校验信息。
- 旧格式只能通过显式迁移生成新版本；历史计算快照不得被原地改写。
- 无法安全升级的项目只能只读打开并返回诊断；不兼容格式必须明确拒绝。

## 5. 项目生命周期

### 5.1 项目对象

项目包含以下层次：

1. **工作草稿**：可编辑的当前项目内容。
2. **项目版本**：在明确里程碑创建的不可变项目状态。
3. **计算快照**：绑定项目版本、数据和计算配置的不可变输入。
4. **计算任务**：对一个计算快照执行方案评价、规划、检查或可靠性分析。
5. **证据与评估**：由计算任务产生的不可变结果证据和质量评估。

自动保存只保护当前草稿，不自动创建永久历史版本。

### 5.2 计算绑定

每个计算快照必须固定：

- 项目版本。
- 数据集版本。
- 参数、变量、目标和约束配置。
- 程序版本与受控扩展版本。
- 随机种子。
- 数值容差和单位规则。
- 输入内容校验信息。
- 计算类型和适用的结果维度。

计算快照创建后，任何输入变化都必须形成新的版本或新的快照，不得改变原快照的含义。

### 5.3 归档、删除与方案应用

- 归档项目不能编辑，也不能提交新计算；已存在的模型、结果和报告仍可查看和导出。
- 删除必须明确确认，并先取消或终止关联任务、校验引用一致性，再执行硬删除。
- 系统不提供回收站语义。
- 任意方案评价结果为临时、面向当前查看的结果，不进入项目永久历史。
- 用户应用选定规划结果时，必须创建新的项目版本，不得覆盖结果来源版本。

## 6. 项目包

完整项目包必须包含：

- 项目模型和配置。
- 工作草稿及项目版本。
- 数据集版本和数据溯源元数据。
- 历史计算结果、证据和评估引用。
- 格式版本。
- 清单和完整性校验信息。

项目包不得包含：

- 账号。
- 权限和查看者名单。
- 会话。
- 全局系统配置。
- 部署环境密钥。

导入约束：

- 导入前必须校验格式、兼容性、清单和完整性。
- 导入不得静默覆盖已有项目。
- 每次导入都创建新的项目身份。
- 导入工程师成为新项目所有者。
- 原项目的账号、权限和会话不随包导入。

## 7. 建模与设备

### 7.1 能源与设备范围

产品版本 1 支持电、热、冷三类能源载体及以下设备或负荷：

| 类型 | 建模对象 |
| --- | --- |
| 供能接口 | 电网连接 |
| 可再生能源 | 光伏 |
| 电储能 | 电池储能 |
| 负荷 | 电负荷、热负荷、冷负荷 |
| 热转换 | 热泵、燃气锅炉 |
| 冷转换 | 电制冷机 |

设备必须支持存量设备与新增建设设备的区分。热储能、冷储能和产品版本 2 设备不属于本版本建模范围。

### 7.2 图形化模型

- 用户通过拖拽方式构建能源系统图。
- 每个设备端口具有明确的能源类型、方向和连接规则。
- 连接必须经过类型、方向和端口兼容性校验。
- 不兼容连接必须被拒绝或生成可定位的诊断。
- 模型图、参数表和计算输入必须保持同一项目版本语义。

### 7.3 工程因素

设备模型必须覆盖下列工程因素：

- 热泵性能随温度变化。
- 光伏性能随温度、辐照度、朝向和倾角变化。
- 电池充放电互斥、荷电状态、衰减、循环寿命和更换。
- 电网购电、售电、分时电价、需量费用、并网容量和禁止反送电约束。
- 热力和冷量分配损失、传输容量和泵耗电。
- 存量设备的运行约束与新增设备的建设约束。
- 用户可在同一规划流程中选择模型精度，精度选择不得改变数据、权限和结果追踪规则。

## 8. 数据约束

### 8.1 计算周期与分辨率

- 年度计算采用标准非闰年 365 天。
- 支持 15 分钟、30 分钟和 1 小时分辨率。
- 数据集必须明确时间戳和固定 UTC 偏移。
- 一个数据集内 UTC 偏移必须固定，不使用夏令时切换。
- 闰年数据必须拒绝，或由明确的转换规则转为标准 365 天格式后再使用。

### 8.2 数据记录

每个数据集必须记录：

- 时间戳。
- 固定 UTC 偏移。
- 数值单位和量纲。
- 数据来源类别。
- 数据版本。
- 许可证或授权信息。
- 溯源和适用范围。

### 8.3 数据校验

导入和计算提交前必须校验：

- 行数是否匹配所选年度分辨率。
- 时间戳是否有效、严格递增且无重复。
- 是否存在缺失值、重复记录或乱序记录。
- 单位、量纲和字段类型是否正确。
- 数值是否在允许范围内。
- 数据集的 UTC 偏移、版本和溯源信息是否完整。

阻断性数据错误必须阻止计算提交，并给出字段、位置、错误代码和修复建议。

### 8.4 单位与货币

| 物理量 | 内部权威单位 | 展示单位 |
| --- | --- | --- |
| 能量 | J | kWh、MWh、GJ |
| 功率 | W | kW、MW |
| 温度 | K | °C、K |

- 内部工程量使用 SI 单位；接口和计算数据必须同时表达数值、单位和量纲。
- 计算过程中不得为展示提前舍入。
- 金额、费率和财务输入使用精确十进制权威表示；求解矩阵可以使用浮点数。
- 每个财务项目必须明确基础货币，只能选择 CNY 或 USD。
- 系统不自动进行货币换算。

## 9. 规划与计算

### 9.1 统一规划任务

方案评价、规划搜索、逐时运行、财务评价、环境评价、工程评价和结果检查属于同一计算任务体系。

计算必须由服务端创建快照并执行，浏览器只能提交受权操作、显示状态和读取结果。浏览器关闭后，后台任务仍可继续运行。

### 9.2 参数、变量、目标与约束

- 每个参数必须具有合法当前值。
- 每个规划变量必须具有合法初始值、上下界和类型。
- 规划配置必须明确变量、目标、约束、初始值、边界和算法配置。
- 约束必须能够定位到模型对象、数据字段或计算维度。
- 不执行用户上传的任意 Python、JavaScript、原生动态库或其他任意代码。
- 自定义表达式只能使用受限语法、白名单函数和显式输入，且必须在计算前完成解析、类型和范围校验。

### 9.3 任意方案评价

- 用户可以使用当前参数值直接评价方案全年运行效果。
- 评价至少覆盖能耗、经济、环境和工程指标。
- 任意方案评价不改变项目永久历史，不覆盖项目版本，也不绕过正式输入校验。
- 评价结果必须保留使用的项目版本、数据版本和计算配置。

### 9.4 后台任务

- 计算任务必须幂等，具备资源隔离和明确的取消、失败、超时和完成状态。
- 普通规划任务默认使用 2 个并发槽；经目标硬件验证后可以提高至 3 个。
- 单个规划任务默认硬超时为 8 小时，管理员可以调整。
- 计算子任务必须运行在受控的隔离求解器子进程中，以支持资源限制、超时、取消和孤立进程清理。
- 计算任务可以在浏览器关闭后继续执行。
- 计算任务只能从不可变快照启动；重试不得改变输入含义。
- 批量任务按已完成子任务恢复；不承诺通用求解器内部断点续算。

### 9.5 任务事实与写入资格

- PostgreSQL 保存任务尝试、租约期限和单调 fencing token 等权威事实。
- Redis 只保存可重建的队列、进度、心跳和缓存状态。
- 租约过期或 fencing token 失效的尝试不得提交进度、结果或状态变更。
- 技术任务状态与业务完成结局分离。技术上完成不等于业务上可推荐。
- 业务结局至少能够区分正常完成、无可推荐方案、无可行多目标方案和批量部分完成。

## 10. 优化、财务、环境与可靠性

### 10.1 多目标规划

- 多目标规划必须明确各目标、约束、权重或 Pareto 结果表达方式。
- 最低税后项目投资 IRR 必须是不可被目标权重抵消的硬约束。
- Pareto 解集必须以图表或表格数据展示，并由用户选择结果。
- 系统不得在没有依据时声称存在唯一最优解。
- 无可行多目标方案、全部候选 IRR 无效、最高唯一 IRR 为负等情况必须分别展示状态和警告。
- 应用所选结果必须创建新项目版本，原版本保持不变。

### 10.2 财务评价

- 新建或扩建设备使用增量现金流评价，不把既有设备沉没成本重复计入新增方案。
- 增量财务规划前必须建立“仅使用已有设备且不新增建设”的基准方案。
- 基准方案必须记录可行性、关键假设、确认人和确认内容的完整性校验信息。
- 财务结果必须区分投资、运行成本、收益、税务、融资、现金流和项目投资 IRR 的计算状态。
- IRR 必须能表达唯一、无解、多解、退化、超出定义域和数值失败等状态，不得用一个空值或成功标记替代。

### 10.3 环境评价

- 运行期碳排放必须明确排放边界、排放因子版本和适用数据。
- 排放因子必须与数据集和计算快照绑定。
- 环境结果必须能够追溯到能源流、排放因子和计算版本。

### 10.4 结果有效性

结果至少分别提供以下四个维度：

1. **物理有效性**：能量守恒、设备约束、容量约束、边界条件和工程残差。
2. **最优性有效性**：求解状态、目标界、Gap 或无法证明最优性的原因。
3. **财务有效性**：现金流和 IRR 的定义域、解的数量及数值状态。
4. **可靠性统计**：不确定性样本、有效样本、无效样本和统计适用范围。

汇总页面只能派生“可用”“受限使用”“不可用”等摘要，不能重新计算、覆盖或隐藏原始维度。

### 10.5 可靠性与敏感性

- **固定方案可靠性**：设备容量和设备选择固定，对每个样本重新优化运行。
- **重规划敏感性**：每个样本都重新优化容量、设备选择和运行。
- 两类分析必须分别命名、分别展示输入和结果，不得合并成一个指标。
- 随机种子、样本输入、参数版本和计算配置必须足以支持重复分析。
- 无效样本必须单独隔离和统计，不得静默计入有效分布。
- 统计结果必须披露有效样本数、总样本数、剔除原因、分母定义和结果适用范围。

## 11. 结果、证据与导出

### 11.1 结果内容

结果页面至少能够查看：

- 年度用电、用热、用冷和能源平衡。
- 设备容量、建设状态、逐时运行和峰值指标。
- 电网购售电、分时费用、需量费用和容量约束结果。
- 投资、运行成本、收益、现金流、税务和 IRR 状态。
- 运行期碳排放及排放边界。
- 物理残差、约束违例、最优性状态和收敛或停止原因。
- 可靠性样本分布、有效率和适用范围。
- 数据版本、模型版本、计算配置、结果状态和关联标识。

### 11.2 证据不可变

- 求解证据、物理检查、最优性检查、财务评估和可靠性评估均为不可变记录。
- 每次重新检查都创建新的评估记录，不覆盖原记录。
- 报告和 Excel 必须引用固定证据和评估记录。
- 查看报告或导出 Excel 时不得静默重新计算。

### 11.3 Excel

- 支持固定模板的 Excel 报告导出。
- 固定标题支持简体中文和英语，默认使用简体中文。
- 导出结果必须注明项目版本、计算快照、数据版本、结果状态和适用范围。
- 查看者可以导出结果型 Excel；只有所有者可以导出完整项目包。

## 12. 软件架构

### 12.1 组件形态

```mermaid
flowchart LR
    B[桌面浏览器] --> RP[统一入口]
    RP --> UI[主界面]
    RP --> TW[独立教程页面]
    UI --> API[模块化应用 API]
    TW --> API
    API --> DB[(PostgreSQL)]
    API --> OBJ[(内容寻址对象存储)]
    API --> Q[事务消息出口]
    Q --> R[(Redis：可重建状态)]
    R --> CW[计算 Worker]
    R --> IW[I/O Worker]
    CW --> S[隔离求解器子进程]
    CW --> DB
    CW --> OBJ
    IW --> DB
    IW --> OBJ
```

### 12.2 组件职责

| 组件 | 设计职责 |
| --- | --- |
| 主界面 | 画布、表单、即时提示、任务状态和结果展示；不直接访问数据库、队列或对象路径 |
| 模块化应用 API | 身份、授权、项目、版本、权威校验、任务提交和跨领域事务编排的唯一业务入口 |
| 计算 Worker | 方案评价、规划、不确定性分析和结果检查 |
| I/O Worker | 数据集处理、Excel、项目包和存储维护；与计算任务使用不同逻辑队列和资源额度 |
| 隔离求解器子进程 | 在任务专属进程中执行数学求解，接受资源限制、超时、取消和清理控制 |
| 独立教程页面 | 无项目数据的只读帮助和教程，可独立更新 |
| PostgreSQL | 项目、权限、任务事实、版本、证据和业务索引的事务事实来源 |
| Redis | 可重建的队列、进度、心跳和缓存状态，不保存唯一业务事实 |
| 内容寻址对象存储 | 大型不可变数据集、快照、中间对象、证据和报告对象 |

### 12.3 数据所有权与依赖

- 业务单元只能通过公开命令端口、只读查询端口或版本化领域事件协作。
- 任何单元不得直接读写其他单元的私有表、ORM 或内部实现。
- 每个权威实体只能有一个写入单元。
- 应用编排层可以安排跨单元事务顺序，但不拥有业务实体。
- Worker 只能在有效任务租约和 fencing token 范围内写入任务、尝试、暂存对象、结果和诊断。
- Worker 不得修改项目、权限、用户或计算输入。
- 主界面不直接访问数据库、Redis、对象路径或 Worker。
- 独立教程页面不接收项目数据、不拥有业务数据。

### 12.4 技术边界

- Python 负责应用 API、后台编排、求解器适配和科学计算。
- TypeScript/React 负责主界面和教程呈现。
- Rust 只用于经过收益证明的窄范围第一方原生基础能力，不承载通用业务层。
- 产品版本 1 不拆分业务微服务，不要求高可用集群。
- 不采用完整事件溯源。
- 不让 Redis 成为任务、结果或权限的唯一事实来源。
- 不把大型时序数组作为数据库 JSON 保存。
- 不允许任意用户上传 Python、JavaScript 或原生动态库。

## 13. 诊断、审计与可维护性

### 13.1 用户诊断

每条用户可见诊断必须包含：

- 稳定诊断代码。
- 严重程度。
- 是否阻断。
- 中文和英语消息键。
- 对象、字段或时间位置。
- 修复建议。
- 关联标识。

后端只提供诊断数据和消息键，不硬编码用户界面展示文案。

### 13.2 审计范围

必须审计：

- 登录和认证结果。
- 窗口接管。
- 账号管理。
- 所有权和权限变化。
- 项目创建、复制、归档、删除和导入导出。
- 草稿、版本和计算快照变化。
- 任务提交、取消、终止和结果应用。
- 维护操作、解锁、所有权转移和清理。

审计记录只保存身份、时间、动作、对象标识、修订号、结果和必要校验信息，不复制密码、令牌、完整模型、完整数据集或原始求解日志。

### 13.3 资源与健康

- 主界面和应用 API 必须保留独立 CPU、内存和 I/O 余量。
- 求解器不得耗尽主机资源或阻塞普通页面。
- 提交计算前必须检查存储安全阈值；空间不足时禁止提交并给出清理建议。
- 系统必须提供存活状态、就绪状态、结构化日志、任务指标、队列指标、存储容量和关联标识。
- 项目数据、密钥和部署配置不得写入日志或公开输出。

## 14. 用户界面、帮助与本地化

- 主界面、快速帮助、教程和 Excel 固定标题支持简体中文与英语，简体中文为默认语言。
- 主界面和教程的术语、参数名称、版本信息和帮助主题必须一致。
- 核心操作必须支持键盘完成。
- 当前焦点必须可见。
- 状态不得只依靠颜色表达，必须同时提供文字、图标、形状或其他非颜色信息。
- 对比度必须满足 WCAG 2.2 AA。
- 桌面浏览器显示范围为 1920×1080 至 4K。
- 浏览器基线为 Chrome 130、Edge 130、Firefox 128 ESR，并兼容发布时的当前稳定版本。
- 独立教程页面在计算服务不可用时仍可提供基础教程和恢复说明。
- 教程页面不包含项目数据，不承担业务计算或权限判断。

## 15. 代表性工程输入示例

以下示例只说明输入组织方式，数值为合成工程数据，不代表客户项目、产品性能承诺或唯一验收值。

### 15.1 场景概况

| 属性 | 示例值 |
| --- | --- |
| 场景 | 小型工业园区 |
| 建筑面积 | 24,000 m² |
| 年运行天数 | 300 天 |
| 计算日历 | 标准非闰年 365 天 |
| 时间偏移 | UTC+08:00 |
| 支持分辨率 | 15 分钟、30 分钟、1 小时 |
| 能源载体 | 电、热、冷 |

### 15.2 设备清单示例

| 设备 | 存量/新增 | 示例规模 |
| --- | --- | --- |
| 电网连接 | 存量 | 1,800 kW |
| 屋顶光伏 | 新增 | 900 kW |
| 空气源热泵 | 新增 | 1,200 kW 热功率 |
| 燃气锅炉 | 存量 | 900 kW 热功率 |
| 电制冷机 | 存量 | 800 kW 冷功率 |
| 电池储能 | 新增 | 1,800 kWh |

### 15.3 时序数据字段

标准时序数据至少包含：

- 时间戳和 UTC 偏移。
- 电负荷、热负荷、冷负荷。
- 环境温度。
- 水平面总辐照度和光伏可用率。
- 分时购电价格。
- 电网排放因子。

字段必须满足时间递增、单位明确、数值范围有效、缺失值阻断计算和版本溯源完整等约束。

## 16. 术语表

| 术语 | 定义 |
| --- | --- |
| 工作草稿 | 当前可编辑的项目状态，自动保存不等于永久版本 |
| 项目版本 | 在明确里程碑创建的不可变项目状态 |
| 计算快照 | 固定项目版本、数据和计算配置的不可变输入 |
| 计算任务 | 对计算快照执行评价、规划、检查或可靠性分析的服务端任务 |
| 证据 | 描述计算输入、执行状态、约束检查和结果依据的不可变记录 |
| 结果评估 | 对物理、最优性、财务或可靠性维度进行的一次不可变评估 |
| 所有者 | 对项目拥有唯一编辑和完整项目包导出权限的工程师 |
| 查看者 | 只能读取项目和已有结果，并可导出结果型 Excel 的工程师 |
| 内容寻址对象 | 由内容校验值定位、不可变且可去重的大型对象 |
| 固定方案可靠性 | 固定设备选择和容量，只对运行进行样本级重新优化 |
| 重规划敏感性 | 对每个样本重新优化设备选择、容量和运行 |

## 17. 编号化产品约束目录

本节给出完整的产品约束索引。编号只是本文内部的稳定标识，不要求读者查阅其他材料。

### 17.1 用户与部署约束

#### REQ-USER-001 用户规模与角色

- 支持不超过 50 个授权用户和不超过 2 个同时在线用户。
- 系统内置管理员和工程师两类角色。
- 管理员可创建、停用、恢复工程师账号并重置密码。
- 自助注册默认关闭；开启时只能注册工程师。
- 默认管理员首次登录必须更换初始密码。
- 不接入外部身份提供方，不接受外部角色自动映射。

#### REQ-USER-002 项目权限与编辑会话

- 项目具有唯一所有者，默认仅所有者可见。
- 只有所有者可以编辑；其他工程师只能被授予查看权限。
- 查看者可以读取模型和已有结果并导出 Excel，但不能修改项目或导出完整项目包。
- 所有权可以转移；转移后原所有者默认成为查看者。
- 管理员通过维护入口只能查看诊断、解锁和转移所有权，不能修改模型或财务参数。
- 每个账号同时只能有一个活动窗口级会话；新窗口接管后旧窗口失效。

#### REQ-USER-003 账号与会话安全

- 密码采用安全哈希，实施最低强度、首次改密、临时密码改密和登录限速。
- 会话具有过期、主动退出和凭据失效语义。
- 账号停用、密码重置和关键凭据变化必须使相关活动会话失效。
- 窗口级凭证与普通登录凭证分离，使接管可以撤销旧页面而不撤销新页面。
- 认证错误不能泄露密码、密码哈希或可用于枚举账号的信息。

#### REQ-DEP-001 部署形态

- 采用桌面浏览器访问的 B/S 应用。
- 部署于 Linux x86-64 个人电脑或等效本地服务器。
- 使用 Docker 封装运行环境。
- 核心功能不依赖 GPU。
- 目标资源约为 8 核 CPU、32 GB 内存和 100 GB 可用存储。

#### REQ-DEP-002 网络与安全边界

- 允许经本机、局域网或互联网访问。
- 应用自身负责账号登录、登录限速、会话和账号保护。
- 传输加密、证书、防火墙、域名和外层访问控制由部署环境负责。
- 应用安全不能以外层网络设施必然存在为前提。
- 不集成外部身份提供方。

#### REQ-DEP-003 离线能力

- 本地服务器和访问网络可用时，互联网断开不得影响核心建模、计算、结果查看和导出。
- 外部气象、公共数据和 AI 服务可以降级。
- 已缓存内置数据和用户上传数据继续可用。
- 系统明确显示离线状态和不可用能力。

#### REQ-DEP-004 本地持久化与维护

- 数据库、项目对象、原始数据、计算结果和日志存储在部署电脑或明确挂载存储中。
- 不默认依赖外部云数据库或云文件存储。
- 容器重建不能造成业务数据丢失。
- 安装、启动、停止、升级和完整部署数据迁移必须具有稳定操作入口。
- 完整迁移覆盖应用数据、持久化对象及恢复所需配置，不另行承诺持续备份频率和恢复时间目标。

#### REQ-DEP-005 本地化与显示环境

- 用户界面、用户可见错误、快速帮助、独立教程和 Excel 固定标题支持简体中文和英语。
- 默认语言为简体中文；用户自定义项目名称和备注不自动翻译。
- 项目基准币种支持 CNY 和 USD，默认 CNY。
- 同一项目只能使用一个明确基准币种，不自动换汇。
- 日期、时间、数字、币种和单位按当前语言及项目设置显示，但权威存储值不因展示格式改变。

#### REQ-DEP-006 客户端范围

- 产品版本 1 只适配桌面浏览器，不要求适配手机和平板。
- 支持 1920×1080 至 4K 显示范围。
- 低于 1920×1080 时可以不保证完整布局，但必须提示最低显示要求。
- 浏览器基线为 Chrome 130、Edge 130 和 Firefox 128 ESR，并兼容发布时的当前稳定版本。

### 17.2 项目约束

#### REQ-PROJ-001 核心使用闭环

- 用户无需修改代码即可完成创建项目、建模、连线、配置数据和参数、评价当前方案、配置规划、校验、提交任务、检查有效性、查看结果、选择多目标结果、执行可靠性分析和导出的完整闭环。
- 多目标结果由用户选择后才能应用。
- 结果应用创建新项目版本，不覆盖原版本。

#### REQ-PROJ-002 自动保存与历史版本

- 编辑过程自动保存，以降低意外丢失。
- 自动保存不创建永久历史版本。
- 手动保存、提交计算、恢复版本和应用结果等明确节点创建永久版本。
- 永久版本可以记录名称、说明、父版本和创建原因。
- 恢复历史版本通过创建新版本和新草稿完成，不倒写历史。
- 每个计算结果关联明确的项目版本、数据版本和计算配置。

#### REQ-PROJ-003 候选方案

- 用户可以复制项目形成独立候选方案。
- 产品版本 1 不提供跨项目多方案并排比较、差异高亮和排序界面。
- 同一多目标任务内的 Pareto 结果展示不属于跨项目方案比较。

#### REQ-PROJ-004 归档与删除

- 项目可以归档和撤销归档。
- 归档状态下不可编辑或提交新任务，但可读取和导出已有内容。
- 产品不提供项目回收站。
- 删除前必须明确确认，取消排队任务，终止运行任务并完成一致性检查。
- 一致性检查完成后执行硬删除；对象清理可以重试，但项目不能恢复可见。

#### REQ-PROJ-005 项目包

- 项目包用于单项目备份、迁移和交换。
- 项目包包含模型、配置、版本、数据集、历史结果、溯源信息、格式版本、清单和完整性校验信息。
- 项目包不包含系统账号、全局权限、会话和全局配置。
- 导入必须校验格式、完整性和兼容性，不得静默覆盖已有项目。
- 导入创建新项目标识，导入工程师成为所有者，原授权关系不迁移。
- 导入后的项目可脱离原部署查看历史结果，并以新本地任务重新计算。
- 只有项目所有者可以导出完整项目包。

### 17.3 模型约束

#### REQ-MODEL-001 能源与设备范围

- 支持电力、供热和供冷三种能源载体。
- 支持光伏、电池储能、电网、电负荷、冷热负荷、热泵、电制冷机和燃气锅炉。
- 冷热负荷为统一设备类型，可配置为供热、供冷或冷热组合负荷。
- 不包含风电、燃气内燃机、余热锅炉、吸收式制冷机、热储能和冷储能。

#### REQ-MODEL-002 可视化建模

- 通过拖拽添加设备，通过连接描述能流。
- 连接明确能源类型、方向和端口。
- 不兼容连接必须阻止或报告，并定位到相关设备和端口。
- 保存后的模型必须能够重新打开并用于计算。

#### REQ-MODEL-003 工程因素

- 热泵模型表达供热、供冷和环境温度影响。
- 电池模型表达充放电互斥、SOC、衰减、循环寿命和更换。
- 光伏模型表达温度、辐照度、朝向和倾角。
- 电网模型表达购售电、峰谷电价、需量电费、并网容量和禁止反送电。
- 热冷输配表达损耗、输送容量和水泵耗电。

#### REQ-MODEL-004 模型精度

- 用户可以按项目或设备选择模型精度等级。
- 产品只有一套统一规划流程，不拆成互不兼容的快速估算和详细计算流程。
- 时间分辨率、模型精度、变量、约束和算法共同决定计算规模。

#### REQ-MODEL-005 存量与增建

- 明确区分已有设备和拟建设备。
- 已有设备容量默认固定；用户可以配置扩容、替换或退役变量。
- 财务评价只评价新建或增建部分的增量现金流。
- 节省购能费用以“不实施新增建设”的基准方案作为收益参照。
- 既有项目沉没成本不能被重复计入新增投资评价。

### 17.4 数据约束

#### REQ-DATA-001 计算周期与分辨率

- 仅支持标准非闰年 365 天的完整年度计算。
- 支持 15 分钟、30 分钟和 1 小时时间步。
- 闰年数据必须先显式转换为标准 365 天格式。
- 年度时间轴使用项目固定 UTC 偏移，不应用夏令时规则。

#### REQ-DATA-002 标准导入

- 时序数据通过标准 CSV 模板导入。
- 模板包含字段说明、单位和示例。
- 校验时间戳、行数、缺失值、重复值、排序、单位和数值范围。
- 错误必须定位到文件、字段和行。
- 阻断性错误未修复前不能提交计算。

#### REQ-DATA-003 内置数据

- 内置数据以标准文件保存并进行版本管理。
- 每份数据记录地区、时间范围、分辨率、单位、许可证和溯源信息。
- 内置数据和上传数据使用相同内部格式与校验规则。

#### REQ-DATA-004 数据来源

- 主要服务中国大陆地区，支持按省、市或可获得的更细地区选择数据。
- 负荷数据主要由用户按模板上传，也可使用内置样例。
- 气象数据可以来自公共查询、内置历史数据或用户上传数据。
- 电价数据可以来自内置数据或用户上传数据；全局内置电价仅由管理员维护。
- 外部查询失败不得阻断与该查询无关的功能。

### 17.5 计算约束

#### REQ-CALC-001 统一规划任务

- 一次任务完成候选搜索、逐时评价、财务评价、环境评价和工程评价。
- 用户不需要手工组合内部子计算。
- 输出设备推荐参数、目标值、逐时评价、财务、环境和工程结果。
- 不生成生产运行计划，不接入实时控制。

#### REQ-CALC-002 变量

- 设备或系统参数可以是固定值或优化变量。
- 新建设备按类型提供默认变量；已有设备默认固定。
- 每个参数必须有合法当前值，每个变量必须有合法初始值。
- 变量初始值必须满足类型、单位、范围和直接参数约束。
- 变量支持连续、整数、枚举和布尔等受控类型。

#### REQ-CALC-003 任意方案评价

- 当前方案评价固定全部当前参数，不优化设备容量或设备参数。
- 使用与规划相同的年度数据、时间步、设备模型和财务口径。
- 至少输出年度用电、用气、用热、用冷、逐时评价、经济、环境和工程指标。
- 评价结果用于查看和校核，不写入项目永久历史。

#### REQ-CALC-004 目标与约束

- 默认单目标为税后项目投资 IRR 最大化。
- 用户可以选择税后资本金 IRR、NPV 或其他兼容指标。
- 碳排放可以作为目标或约束。
- 支持设备级和系统级约束。
- 默认不允许未满足负荷；只有用户明确启用、设置惩罚、上限和适用负荷后才允许负荷削减，且结果必须显著报告。
- 简单模式使用预定义约束；高级模式使用受限代数表达式。
- 表达式必须校验语法、变量、单位、量纲和受支持运算，不能执行用户脚本。

#### REQ-CALC-005 算法选择

- 规划引擎支持多个算法并存。
- 默认根据目标、约束、变量和问题规模自动选择兼容算法。
- 用户可以切换为手动选择。
- 不兼容算法不能提交，并必须说明原因。
- 结果记录算法标识、版本、参数、停止条件和随机种子。

#### REQ-CALC-006 多目标

- 支持加权法、优先级法、ε-约束法和 Pareto 解集。
- 只展示与当前模型和算法兼容的方法，并说明参数含义。
- 所有多目标规划都必须设置最低税后项目投资 IRR 硬约束。
- 折现率与最低可接受 IRR 是两个不同字段和语义。
- 没有唯一有效项目 IRR 的候选不能进入正常 Pareto 解集。
- Pareto 结果由用户选择；系统只能按公开可复现规则标记参考解，不能代替用户选择。
- 没有候选满足硬约束时，任务以“完成但无可行多目标方案”结束，不自动降低门槛。

#### REQ-CALC-007 校验门禁

- 提交前必须校验模型、连接、数据、变量、目标、约束、算法能力和财务基准确认状态。
- 模型不完整、连接非法、数据错误或配置冲突时禁止提交。
- 一次返回已发现问题，并定位到设备、连接、字段、数据行或配置项。
- 诊断区分阻断错误和普通警告；警告不能降级阻断错误。

#### REQ-CALC-008 后台任务

- 当前方案评价、规划和不确定性分析均在后台执行。
- 关闭页面不终止任务；重新访问后可以读取排队、运行、完成、取消、超时或失败状态。
- 用户可以取消未结束任务。
- 重复提交必须要求确认、拒绝重复或复用已有任务。
- 支持至少 2 个并发规划任务，目标硬件验证后可以配置为 3 个；超出资源的任务排队。
- 长任务不能阻塞项目编辑、浏览和已完成结果查看。
- 单任务默认最长运行 8 小时，管理员可以调整。

#### REQ-CALC-009 收敛与停止

- 不使用适用于所有算法和物理量的统一全局残差百分比。
- 每种算法和物理量具有独立残差、容差、尺度、归一化和停止条件。
- 收敛不等于全局最优。
- 能提供最优性界限、Gap 或证明时必须记录。
- 达到时间上限时保存已有可行解、最优性信息和停止原因。

#### REQ-CALC-010 失败诊断

- 失败、中止和超时保存任务标识、时间、停止阶段、用户原因、必要技术诊断、已有可行候选、可行性或最优性信息以及恢复建议。
- 诊断必须关联项目版本、数据版本和计算配置。
- 诊断不得泄露其他项目或其他用户数据。

### 17.6 财务与环境约束

#### REQ-FIN-001 评价边界与周期

- 财务评价采用项目明确的评价周期和基准币种。
- 新建和增建设备采用增量现金流。
- 基准方案是不新增建设、仅使用已有设备的可行方案。
- 基准方案的关键假设必须由用户确认并进入计算快照。

#### REQ-FIN-002 收益

- 收益至少能够表达节省购能费用、售能收入和适用的其他项目收益。
- 收益口径必须区分实际现金流与仅用于展示的技术指标。
- 任何收益必须能够追溯到能流、价格、期间和计算规则版本。

#### REQ-FIN-003 成本

- 成本至少覆盖设备投资、安装或建设、运行维护、能源购买、替换和残值相关项目。
- 存量资产成本与新增投资成本必须分开。
- 成本的发生时间、计价单位、税务属性和现金流归属必须明确。

#### REQ-FIN-004 税务与融资

- 项目投资和资本金口径必须区分。
- 税务、折旧、融资、贷款、利息和还本的输入与结果必须具有明确口径。
- 税后项目投资 IRR 和税后资本金 IRR 不能混为同一指标。

#### REQ-FIN-005 IRR 异常

- IRR 结果必须区分唯一解、无解、多解、退化、超出定义域和数值失败。
- 不能把所有异常折叠为缺失值或单一失败标志。
- 最高唯一 IRR 为负、全部候选无有效 IRR 和多目标无可行方案必须产生不同业务结局。

#### REQ-ENV-001 运行期碳排放

- 运行期碳排放明确核算边界、能源流、排放因子、地区、时间范围和因子版本。
- 排放因子与数据集和计算快照绑定。
- 环境指标可以作为目标、约束或结果，但不能改变财务指标定义。

### 17.7 有效性与不确定性约束

#### REQ-VALID-001 解有效性检查

- 每个规划结果必须执行与任务类型匹配的有效性检查。
- 物理、最优性、财务和可靠性结论必须独立记录。
- 缺少所需证据时不能判定相应维度通过。
- 检查不得修改原始求解结果。

#### REQ-VALID-002 MILP 有效性

- 记录原始求解状态、可行性、目标值、界、相对 Gap 和停止原因。
- Gap 只在其数学定义有效时给出。
- 零目标、异号界、缺失界和未证明最优必须有明确状态，不得伪造数值。
- 物理约束检查与求解器状态分开表达。

#### REQ-VALID-003 MINLP 有效性

- 记录原始求解状态、约束违例、物理残差、尺度、容差和停止原因。
- 不同物理量采用适用的归一化和容差。
- 局部收敛、可行但未证明全局最优、数值失败和证据不足必须分别表达。

#### REQ-REL-001 不确定输入与分析模式

- 不确定性分析是用户按需启动的可选功能。
- 支持固定方案可靠性和重规划敏感性两种不同模式。
- 不确定输入必须声明变量、分布、参数、相关性、单位和适用范围。
- 分布设置必须在创建任务前校验。

#### REQ-REL-002 样本求解

- 固定方案可靠性固定设备选择和容量，只重新优化样本运行。
- 重规划敏感性对每个样本重新优化设备选择、容量和运行。
- 样本使用可复现种子，并绑定原始规划结果和不可变输入。
- 样本失败或无效不能改变原规划结果。

#### REQ-REL-003 统计结果

- 统计结果披露总样本数、有效样本数、无效样本数、失败原因和分母。
- 输出适用的分布、分位数、可靠性指标和配置变化范围。
- 无效样本单独报告，不静默进入有效分布。
- 统计结果必须声明适用范围和限制。

#### REQ-REL-004 批量任务

- 不确定性分析使用父任务和样本子任务。
- 子任务共享全局计算资源额度，不能绕过并发和存储门禁。
- 支持取消传播、部分完成、有限重试和按已完成样本恢复。
- 不承诺通用求解器进程内部断点续算。

### 17.8 结果、导出与帮助约束

#### REQ-RESULT-001 首页指标

- 首页提供规划结果摘要、设备配置、财务、环境、工程和状态信息。
- 摘要必须展示项目版本、计算状态、业务结局和四维质量结论。
- 任一维度失败或受限时，摘要必须显著显示，不能用综合成功掩盖。

#### REQ-RESULT-002 工程指标

- 工程结果包括能源平衡、设备容量、逐时运行、峰值、购售电、负荷满足、约束违例和必要残差。
- 指标带定义版本、单位、适用范围和数据引用。
- 报告模板和页面只展示指标，不自行重新计算。

#### REQ-RESULT-003 页面展示边界

- 页面显示用户需要的阶段、进度、状态、停止原因、诊断和最终结果。
- 不要求向普通用户展示算法内部迭代细节。
- 结果页面区分技术任务状态和业务完成结局。
- 结果应用前显示参数差异和来源版本，并要求用户确认。

#### REQ-EXPORT-001 Excel 报告

- Excel 报告固定引用一个证据包和一个结果评估，不在导出时重新求解。
- 固定标题支持中英文。
- 报告至少注明项目版本、数据版本、计算配置、算法、结果状态、单位和适用范围。
- 查看者可以导出获授权结果的 Excel，只有所有者可以导出完整项目包。

#### REQ-HELP-001 程序内快速帮助

- 核心页面、按钮、参数、错误和结果具有稳定帮助主题。
- 快速帮助与当前程序版本、参数元数据和术语一致。
- 帮助内容支持中英文，并在离线条件下可读。

#### REQ-HELP-002 独立教程

- 教程是独立静态页面，不接收项目数据，不拥有业务数据。
- 主程序通过稳定主题进入教程。
- 教程声明适用程序版本，并与快速帮助共享术语和参数元数据。
- 计算服务不可用时，教程仍能提供基础内容和恢复说明。

### 17.9 非功能约束

#### REQ-NF-001 可复现性

- 计算固定模型、数据、配置、程序版本、扩展版本、随机种子、容差和内容校验信息。
- 相同输入在声明容差内应产生可复现结果。
- 随机算法和不确定性样本必须记录种子及采样配置。

#### REQ-NF-002 可解释性与追踪

- 结果能够追踪到项目版本、数据集、模型、算法、配置、证据和评估。
- 诊断使用稳定代码、位置、严重程度、阻断标志、消息键和关联标识。
- 审计覆盖身份、权限、项目生命周期、任务、导入导出和结果应用。

#### REQ-NF-003 易用性

- 普通工程师无需修改代码即可完成核心使用闭环。
- 校验一次返回多项问题并提供定位和修复建议。
- 核心流程支持键盘操作、可见焦点和非颜色状态表达。
- 用户界面满足 WCAG 2.2 AA 对比度。

#### REQ-NF-004 可扩展性

- 设备、算法、数据源和指标通过受控注册扩展。
- 扩展必须声明标识、版本、兼容性、能力、单位、参数和迁移规则。
- 扩展不能访问用户会话、任意数据库连接或任意服务器路径。
- 只加载随产品安装并经过测试的受控扩展。

#### REQ-NF-005 并发与资源

- 支持 2 个同时在线用户和至少 2 个并发规划任务。
- 普通页面和应用接口保留独立资源余量。
- 计算和 I/O 使用独立队列、任务类型白名单和资源额度。
- 求解器运行在任务专属隔离子进程中，接受超时、取消和资源限制。

#### REQ-NF-006 存储与容量

- 提交任务前估算存储需求并检查安全阈值。
- 空间不足时阻止提交并给出清理建议。
- 完整逐时结果按明确保留规则保存；样本任务允许保留摘要和选择性逐时数据。
- 被项目版本、结果、报告或项目包引用的对象不能清理。
- 内容去重不能突破项目授权边界。

#### REQ-NF-007 原生基础模块边界

- Python 负责应用 API、任务编排、求解器适配和科学计算。
- TypeScript/React 负责主界面和教程呈现。
- Rust 只用于窄范围、第一方、可独立证明收益的基础能力。
- 不建立通用 Rust 业务层，不把领域所有权分散到跨语言实现中。

## 18. 产品完成边界

### AC-MVP-001 全年冷热电系统规划

- 用户能够在标准 365 天数据下完成电、热、冷系统建模、当前方案评价、规划、结果查看和导出。
- 15 分钟、30 分钟和 1 小时三种分辨率均遵循同一模型、单位和结果语义。
- 关键能量守恒、容量约束和工程结果具有明确参考值及容差。

### AC-MVP-002 求解有效性

- MILP 结果能够表达可行性、目标值、界、Gap、停止原因和未证明最优状态。
- MINLP 结果能够表达约束违例、物理残差、尺度、容差和局部或全局状态。
- 财务结果能够区分 IRR 的唯一、无、多、退化、域外和数值失败状态。

### AC-MVP-003 不确定性分析

- 固定方案可靠性只改变不确定输入并重新优化运行。
- 重规划敏感性允许容量、设备选择和运行随样本重新优化。
- 相同种子和输入能够重现样本；无效样本被单独隔离。
- 统计分母、有效率、失败原因和适用范围完整披露。

### AC-MVP-004 工程证据

- 每个核心工程场景具有固定、合法、可复现的输入。
- 每个关键指标具有定义、参考结果和容差。
- 工程基准同时覆盖可手算或可枚举合成案例，以及脱敏代表性工程案例。
- 合成案例用于确定性边界，代表性案例用于整体工程适用性确认。

### AC-MVP-005 账号、权限与生命周期

- 首次改密、登录限速、会话失效、认证审计和窗口接管语义完整。
- 所有者唯一编辑、查看者只读、Excel 导出和项目包所有者专属导出边界完整。
- 所有权主动转移、停用账号后的管理员转移和管理员只读维护边界完整。
- 自动保存、永久版本、结果应用新版本、归档、删除和项目包导入语义完整。

### AC-MVP-006 数据、离线与运维

- 三种年度分辨率均能完成导入、校验、定位和计算。
- 固定 UTC 偏移与闰年显式转换规则完整。
- 互联网断开时仍可完成建模、计算、结果查看和导出。
- 存储估算、容量门禁、清理、容器重建和完整迁移不丢失业务数据。

### AC-MVP-007 多目标与结果应用

- 最低税后项目投资 IRR 始终作为硬约束。
- Pareto 图表、表格、参考规则和用户选择语义完整。
- 无可行方案、无有效 IRR 和最高唯一 IRR 为负具有不同状态与警告。
- 应用所选结果创建新版本，原版本保持不可变。

### AC-MVP-008 帮助、国际化与可用性

- 简体中文和英语均能完成核心使用闭环。
- 用户错误、Excel 标题、快速帮助和教程术语一致。
- 核心流程可使用键盘完成，焦点可见，状态不只依靠颜色。
- 页面、应用接口和任务进度在规定局域网与并发条件下保持可用。

## 19. 业务单元与唯一写入边界

### U01 身份与系统管理

- 拥有账号、密码、角色、首次改密、注册开关、账号状态、窗口凭证、会话接管和全局安全配置。
- 不拥有项目所有权、项目内容或计算结果。
- 对外提供认证主体、账号状态、活动窗口状态和认证审计事件。

### U02 项目访问与所有权

- 拥有项目所有者、查看授权、所有权转移、管理员维护访问和访问判定。
- 不拥有共享编辑、项目内容或计算任务。
- 对外提供访问决策、所有权事件和维护访问审计。

### U03 项目、草稿与版本

- 拥有项目生命周期、项目元数据、数据集绑定、综合草稿修订、自动保存协调、不可变版本、复制、归档、恢复、结果应用和删除协调。
- 不直接修改系统图、计算配置、身份、数据文件内容、指标或求解结果。
- 对外提供项目视图、草稿修订、项目版本、版本来源和生命周期事件。

### U04 系统模型

- 拥有规范系统图、设备实例、端口、连接、能源类型、设备参数、模型精度、布局和拓扑规则。
- 不拥有具体画布库、规划算法或指标计算。
- 对外提供业务拓扑、布局、模型诊断和设备能力需求。

### U05 数据集

- 拥有模板、上传隔离、标准化、校验、分辨率转换、数据版本、内置数据和外部数据适配。
- 不拥有项目拓扑、规划配置或结果指标。
- 对外提供不可变数据集版本、质量报告和溯源证据。

### U06 参数、目标与约束

- 拥有参数当前值、变量、范围、目标、最低 IRR 硬约束、预定义约束、受限表达式和量纲校验。
- 不执行规划求解，也不拥有设备物理方程。
- 对外提供规范计算配置、能力需求和诊断。

### U07 项目校验

- 聚合模型、数据、参数、目标、约束、算法能力和财务基准确认证据。
- 执行保存时校验和计算前完整预检。
- 不修改配置，不忽略阻断错误，不执行计算后证据检查。
- 对外提供版本化校验报告和提交许可。

### U08 任务与资源调度

- 拥有计算快照组装、幂等任务创建、任务状态机、业务完成结局、队列、父子任务、配额、任务尝试、租约、fencing token、进度、取消、超时和有限重试。
- 不拥有规划数学逻辑、工程指标或结果展示。
- 对外提供计算快照、任务、尝试、资源租约、持久进度、停止记录和调度事件。

### U09 方案与运行评价

- 拥有固定建设方案和当前参数的完整年度逐时评价。
- 在固定方案可靠性样本中只重新优化运行变量。
- 不重新选择建设容量，不保存永久项目版本。
- 对外提供逐时运行、物理流、指标输入、原始求解状态和检查证据。

### U10 规划引擎

- 拥有问题构建、算法能力匹配、候选搜索、多目标方法、逐时评价反馈、停止条件和求解器证据。
- 不拥有任务排队、项目权限或结果页面。
- 对外提供候选、Pareto 集、参考解、原始求解状态、最优性证据和逐时结果引用。

### U11 指标与财务有效性

- 拥有财务、环境和工程指标定义与计算，增量财务基准方案、用户确认证据、IRR 分类和财务有效性。
- 不执行候选搜索，不拥有任务状态或结果页面。
- 对外提供带版本的指标、现金流、基准确认、碳排依据和财务有效性结论。

### U12 物理与最优性检查

- 拥有计算后物理可行性、MILP Gap、MINLP 物理残差、最优性证明和证据完整性检查。
- 不做财务判断、可靠性统计或原始求解结果修改。
- 对外提供物理可行性和最优性证明两个独立维度。

### U13 不确定性分析

- 拥有不确定性任务快照、分布、采样、子任务计划、固定方案可靠性、重规划敏感性、样本有效性和统计汇总。
- 不改变原规划结果，不混淆两种分析模式，不绕过任务调度。
- 对外提供样本计划、样本记录、部分统计、可靠性统计和配置变化分布。

### U14 结果、选择与报告

- 拥有统一证据提交、不可变证据包、不可变评估、结果索引、四维汇总、业务结局展示、Pareto 展示、用户选择、差异预览、Excel 和短期下载授权。
- 不重新计算指标，不生成原始求解状态，不改写证据或评估，不直接修改项目。
- 对外提供结果视图、结果选择、报告对象和下载授权。

### U15 项目包

- 拥有版本化清单、流式导出、完整性校验、兼容性检查、格式迁移和导入提交提案。
- 不直接创建项目或所有权，不导入账号、授权或系统配置，不覆盖已有项目。
- 对外提供项目包、导入预检、暂存对象和按唯一写入者分区的导入提案。

### U16 审计、对象与运维

- 拥有审计、对象元数据与引用、配额、两阶段清理、完整性、离线状态、健康状态、备份清单和运维诊断。
- 不修改业务实体，不替代部署环境的证书和防火墙，不定义额外持续备份目标。
- 对外提供审计查询、存储视图、清理记录、健康状态和一致备份清单。

## 20. 关键数据对象语义

### 20.1 窗口会话

窗口会话至少包含用户、凭证版本、签发时间、过期时间、最后活动时间、状态和替代会话标识。

允许状态：

- `active`：当前唯一活动窗口。
- `takeover_pending`：接管中，旧窗口停止新写入。
- `revoked`：已被接管、退出或管理员撤销。
- `expired`：已超过会话期限。

每个账号最多存在一个 `active` 状态。

### 20.2 项目访问决策

访问决策至少包含用户、项目、访问角色、能力集合、决策版本和可选过期时间。

- `owner`：编辑、提交计算、应用结果、管理查看者和导出项目包。
- `viewer`：只读模型和已有结果，可导出获授权 Excel。
- `maintenance_admin`：只读诊断、解锁和所有权转移，不含业务编辑。

对象下载使用短期、单对象授权，不能仅凭对象标识下载。

### 20.3 草稿命令

草稿修改采用语义命令，不采用浏览器整图覆盖。命令至少包含：

- 幂等命令标识。
- 项目标识。
- 预期综合修订号。
- 发起窗口会话。
- 唯一写入单元。
- 命令类型和负载。

相同命令重试返回原结果；预期修订过期时返回冲突。领域修改和综合草稿修订递增必须位于同一数据库事务。

### 20.4 项目版本

项目版本至少固定：

- 项目标识和版本序号。
- 名称、说明、创建者和创建时间。
- 创建原因、父版本和可选来源结果。
- 模型拓扑、界面布局、数据集版本和计算配置。
- 语言、货币、单位系统和固定 UTC 偏移。
- 受控扩展清单、模式版本和内容校验信息。

项目版本不可原地修改。恢复和结果应用都创建新版本。

### 20.5 系统图

系统图至少包含：

- 模式版本。
- 设备实例、设备类型及版本。
- 参数、模型精度和存量/新增状态。
- 端口、连接、能源类型和方向。
- 连接属性、拓扑规则和布局对象。
- 内容校验信息。

拓扑是计算事实，布局是显示事实；布局变化不能改变工程语义。

### 20.6 数据集版本

数据集版本至少固定：

- 模式版本、数据集类型和数据版本。
- 标准非闰年时间轴、分辨率和固定 UTC 偏移。
- 字段、单位、量纲和标准化对象。
- 数据质量报告、溯源、许可证和适用范围。
- 内容校验信息。

上传文件先进入隔离区，只有校验和标准化完成后才能成为可绑定的数据集版本。

### 20.7 计算配置

计算配置至少固定：

- 参数当前值。
- 优化变量、类型、初始值和边界。
- 目标及其方向。
- 预定义约束和受限表达式约束。
- 最低税后项目投资 IRR 硬约束。
- 算法选择模式和算法参数。
- 容差、尺度、归一化和随机种子。

配置必须先通过类型、单位、量纲、范围、算法兼容性和财务基准确认校验。

### 20.8 计算快照

计算快照是任务唯一输入，至少固定：

- 项目版本及内容校验信息。
- 系统图、数据集和计算配置引用。
- 程序、设备、算法、指标、规则和受控扩展版本。
- 财务基准确认证据。
- 任务类型、种子、容差和适用能力清单。

Worker 不读取可变草稿，只读取快照及其不可变对象。

### 20.9 任务尝试与租约

每个任务尝试至少包含任务、尝试序号、当前租约、fencing token、开始时间、最后心跳、停止原因和终态。

- PostgreSQL 中的当前尝试和 token 决定写入资格。
- Redis 中的队列、进度和心跳可以重建。
- 迟到或过期尝试不得提交任何进度、证据或结果。
- 相同任务消息可以至少一次投递，处理器必须幂等。

### 20.10 求解证据包

求解证据包至少固定：

- 任务和当前有效尝试。
- 计算快照。
- 算法标识、版本、参数、种子和停止条件。
- 原始求解状态、候选或 Pareto 索引。
- 指标对象、逐时结果对象和诊断。
- 清单及内容校验信息。

证据包不可变；导入证据保留其原部署命名空间，不伪造本地历史任务。

### 20.11 结果评估与索引

每个结果评估至少包含：

- 评估标识、证据包标识和评估规则版本。
- 物理有效性。
- 最优性有效性。
- 财务有效性。
- 可靠性统计状态。
- 评估时间、诊断和适用范围。

结果索引只更新最新评估引用和业务完成结局，不覆盖历史评估。报告固定证据包和评估标识。

### 20.12 结果选择

结果选择至少包含：

- 结果和所选解标识。
- 选择用户和选择类型。
- 可选参考规则。
- 参数差异补丁。
- 用户确认前预览内容的校验信息。

结果单元只生成选择和差异预览；项目单元在确认预览未变化后创建新版本。

### 20.13 不确定性快照

不确定性快照至少固定：

- 基础规划结果和计算快照。
- 分析模式。
- 不确定变量、分布、参数、相关性和单位。
- 样本数、种子和采样配置。
- 任务资源配置和逐时结果保留策略。

每个样本记录输入、子任务、有效性、失败原因和输出引用。统计汇总不覆盖样本事实。

### 20.14 项目包导入提案

导入提案至少包含：

- 包清单校验信息。
- 原格式版本和迁移后格式版本。
- 已暂存对象。
- 拟创建项目快照。
- 按所有权单元分区的提交内容。
- 校验报告、幂等键和过期时间。

I/O Worker 只校验、迁移和暂存；项目、所有权、模型、数据、配置、结果索引和对象引用由各自唯一写入单元在一个事务中提交。

## 21. 跨单元协作规则

### 21.1 通用规则

- 可变实体使用稳定不可猜测标识和单调递增修订号。
- 不可变对象携带模式版本、内容校验信息和创建来源类别。
- 时间点以 UTC 保存，项目同时保存固定 UTC 偏移。
- 跨边界工程量同时传递数值、单位和量纲。
- 创建任务、导入项目、应用结果和完成对象提交必须接受幂等键。
- 事务消息允许至少一次投递，所有消费者必须幂等。

### 21.2 同步协作

- 应用编排层可以调用多个业务单元完成一个用户命令，但不拥有领域数据。
- 项目校验读取模型、数据、配置、算法能力和财务基准确认，不直接修改这些对象。
- 任务调度读取项目版本、模型、数据、配置、算法能力和基准证据，组装不可变计算快照。
- 规划引擎可以调用运行评价和指标计算，但不能直接写结果索引。
- 不确定性分析生成子任务计划，任务调度负责创建和调度子任务。
- 结果单元校验当前任务尝试写入资格后，接收完整证据并固化结果。

### 21.3 只读结果流

- 系统模型、数据集和计算配置向项目校验提供只读快照。
- 运行评价向规划、指标和物理检查提供只读输出。
- 规划引擎向指标、最优性检查和结果固化提供候选及证据。
- 财务、物理、最优性和可靠性结论向结果单元提供只读评估输入。
- 项目版本、数据集和结果证据向项目包提供只读导出输入。

读取不授予写权限；消费者不得修正或覆盖生产者的权威结论。

### 21.4 事件与提交

- 业务写入与脱敏领域事件写入处于同一数据库事务。
- 审计和运维只消费脱敏事件或受控查询，不获得业务写权限。
- 导入提案按唯一写入单元分区，再由应用编排层原子提交。
- 导入的历史任务仅作为证据来源记录，不进入本地活动任务状态机。
- 结果选择由应用编排层校验后交给项目单元创建版本，结果单元不直接修改项目。

### 21.5 禁止依赖

- 主界面或教程直接访问数据库、Redis、对象路径或 Worker。
- Worker 读取可变草稿或修改项目版本、权限和账号。
- 任一业务单元直接写入另一单元的权威表。
- 设备或算法扩展访问用户会话、任意数据库连接或任意服务器路径。
- 报告模板、页面组件或教程实现财务和工程指标计算。
- 不确定性分析绕过任务调度启动样本。
- 重规划敏感性结果被标记为固定方案可靠性。
- 结果汇总使用一个总等级覆盖四维原始结论。
- 导出时重新求解、重新计算指标或修改结果评估。
- Redis、日志或实时进度消息成为唯一业务事实。

## 22. 状态与业务结局

### 22.1 技术任务状态

任务至少支持：

- 排队。
- 运行。
- 完成。
- 取消中。
- 已取消。
- 超时。
- 失败。

技术完成只表示执行链正常结束，不自动表示得到可推荐方案。

### 22.2 业务完成结局

业务结局至少支持：

- 正常完成。
- 无可推荐方案。
- 无可行多目标方案。
- 批量部分完成。
- 仅产生受限使用结果。
- 证据不足，结果不可用。

技术状态与业务结局必须分别保存和展示。

### 22.3 四维结果状态

每个维度至少区分：

- 已通过。
- 受限或未证明。
- 失败。
- 不适用。
- 证据不足。

财务维度另外区分 IRR 唯一、无解、多解、退化、域外和数值失败；可靠性维度另外区分未执行、部分完成和统计有效性不足。

## 23. 存储与对象生命周期

### 23.1 对象写入

- 大型对象先写临时区，完成内容校验后原子提交并建立数据库引用。
- 任何业务记录不得引用半成品对象。
- 对象可以按内容去重，但授权、保留和删除按业务引用判断。
- 相同内容不能成为跨项目越权访问的依据。

### 23.2 对象保留

下列对象被引用期间不得清理：

- 项目版本引用的模型、布局、数据集和配置。
- 计算快照引用的输入对象。
- 证据包引用的逐时结果和指标对象。
- 结果评估、报告和 Excel 引用的证据。
- 项目包导出所需的版本、数据和结果对象。

### 23.3 存储门禁

- 任务提交前估算快照、中间结果、逐时结果和样本结果的存储需求。
- 可用空间低于安全阈值时阻止任务并给出清理建议。
- 样本任务可以保留统计摘要和选择性逐时结果，但保留策略必须写入任务快照。
- 自动清理必须可审计，且不能清理仍被引用的对象。

### 23.4 删除与清理

删除顺序固定为：

1. 请求删除并确认。
2. 取消排队任务。
3. 终止运行任务。
4. 校验项目、结果、报告和对象引用一致性。
5. 硬删除项目业务数据。
6. 重试清理无引用对象。

一致性确认前不得硬删除。删除后审计只保留最小事件元数据，不保留项目正文。
